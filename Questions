Q: Intelij - de ce de fiecare data trebuie sa intru la Open Module Settings sa repar caile catre foldere?
A: gradle strica buildul

Q: Cum interpretam <T> inainte de o metoda generica: <T> MyColl<T> myMethod(){}
A: <T> dinaintea metodei anunta metoda ca se va folosi tipul T in ea, iar ea nu are nici o declaratie a vreunei variablie de tip T in ea

Q: PECS (Producer extends | Consumer super) - who is who?
* A: am inteles. - De facut un answer pe StackOverflow sa verific ca e corect ce am inteles. *

Q: Box<T> s potriveste bine ca decorator pt T?
A: da

Q: In ce directie se modifica obiectele unele pe altele? 
De obicei daca am 2 obiecte x y atunci amandoua se vor contine pe ele insle. Mi se pare ciudat
A: 

Q: http://jqno.nl/equalsverifier/manual/final/ - de ce sa fac clasele finale, cand da si cand nu?
A:

Q: Cum extind JPArepository fara sa rescriu metodele default implementate de Spring Data JPA
A: 

Q: How Lazy loading happens?
A: You create an object with null variables then when you need it use getInstance which instantiates the variables with values.

App: 

presentation tier:
 - webpage (client side - Postman for now)
 
app tier:
 - java web app (server side)
 * web API swagger *
 * SPRING app with MVC architecture *
 * hibernate - java based config *
 * Gradle - for dependency management and build *
 * Jenkins - for continuous integration & deployment *
  
data tier:
 - MySQL database 
 
 my app tips:
  - Make objects lazily loaded
